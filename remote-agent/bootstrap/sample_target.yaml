apiVersion: fabric.symphony/v1
kind: Target
metadata:
  name: remote-demo3
spec:
  displayName: remote-demo3
  scope: default
  components:
  - name: remote-agent
    type: remote-agent
    properties:
      mock: mock
  topologies:
  - bindings:
    - provider: providers.target.script
      role: script
      config:
        hello: hello
    - provider: providers.target.remote-agent
      role: remote-agent