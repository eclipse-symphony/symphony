apiVersion: fabric.symphony/v1
kind: Target
metadata:
  name: remote-demo5
spec:
  displayName: remote-demo5
  scope: default
  components:
  - name: remote-agent
    type: remote-agent
    properties:
      mock: mock
  topologies:
  - bindings:
    - provider: providers.target.script
      role: script
    - provider: providers.target.remote-agent
      role: remote-agent