apiVersion: federation.symphony/v1
kind: Catalog
metadata:
  name: site-instance
spec:  
  siteId: hq
  type: instance
  properties:
    spec: 
      solution: site-app
      target:
        selector:
          group: site