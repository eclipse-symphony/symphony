apiVersion: federation.symphony/v1
kind: Catalog
metadata:
  name: site-catalog-v1
spec:
  version: v1
  rootResource: site-catalog
  type: catalog
  properties:
    metadata: 
      name: web-app-config:v1
    spec: 
      type: config
      properties:
        image: "ghcr.io/eclipse-symphony/sample-flask-app:latest"
        serviceType: "LoadBalancer"