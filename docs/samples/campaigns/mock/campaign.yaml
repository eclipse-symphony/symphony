apiVersion: workflow.symphony/v1
kind: Campaign
metadata:
  name: mock-campaign
spec:
  firstStage: "mock"
  selfDriving: true
  stages:
    mock:
      name: "mock"
      provider: "providers.stage.mock"
      stageSelector: "$if($lt($output(foo), 5), mock, '')"