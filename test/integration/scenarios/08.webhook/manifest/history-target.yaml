apiVersion: fabric.symphony/v1
kind: Target
metadata:
  name: history-target 
spec:  
  topologies:
  - bindings:
    - role: config
      provider: providers.target.configmap
      config:
        inCluster: "true"