apiVersion: federation.symphony/v1
kind: Catalog
metadata:
  name: siteinstance-v1
spec:
  rootResource: siteinstance
  catalogType: instance
  properties:
    metadata:
      name: siteinstance:v1
    spec: 
      solution: siteapp:v1
      scope: SCOPENAME
      target:
        selector:
          group: site