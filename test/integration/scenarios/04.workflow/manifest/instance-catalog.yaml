apiVersion: federation.symphony/v1
kind: Catalog
metadata:
  name: site-instance
spec:
  type: instance
  properties:
    spec: 
      solution: site-app
      scope: SCOPENAME
      target:
        selector:
          group: site