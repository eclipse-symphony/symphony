apiVersion: federation.symphony/v1
kind: Catalog
metadata:
  name: schema-v1
spec:
  version: v1
  rootResource: schema
  type: schema
  properties:
    spec:
      rules:
        email:
          pattern: "<email>"
        bar:
          expression: "${{$and($gt($val(),10),$lt($val(),20))}}"