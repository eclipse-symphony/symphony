apiVersion: federation.symphony/v1
kind: Catalog
metadata:
  name: config-v-v2
spec:
  rootResource: wrongconfig
  catalogType: config
  rootResource: config
  metadata:
    schema: schema:v1
  properties:
    person:
      bar:
        foo: 0
    email.bar.foo: invalid