apiVersion: federation.symphony/v1
kind: CatalogVersion
metadata:
  name: config-v-version2
spec:
  rootResource: wrongconfig
  catalogType: configVersion
  metadata:
    schema: schema:version1
  properties:
    person:
      bar:
        foo: 0
    email.bar.foo: invalid