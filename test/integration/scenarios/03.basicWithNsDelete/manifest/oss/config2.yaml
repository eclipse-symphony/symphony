apiVersion: federation.symphony/v1
kind: CatalogVersion
metadata:
  name: config2-v-version1
spec: 
  rootResource: config2
  catalogType: config
  properties:
    image: "${{$config('config1:version1','image')}}"
