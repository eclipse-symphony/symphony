{{- if .Values.remoteAgent.remoteCert.used }}
apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: {{ include "symphony.apiClientCATrustBundle" . }}
  namespace: cert-manager
spec:
  sources:
    - secret: 
        name: {{ .Values.remoteAgent.remoteCert.trustCAs.secretName }}
        key: {{ .Values.remoteAgent.remoteCert.trustCAs.secretKey }}
  target:
    configMap:
      key: {{ include "symphony.apiClientCATrustBundleKey" . }}
{{- end }}