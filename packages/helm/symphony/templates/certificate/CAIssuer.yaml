apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "symphony.apiCAIssuerName" . }}
  namespace: {{ .Release.Namespace }}
spec:
  ca:
    secretName: {{ include "symphony.apiServingCertName" . }}